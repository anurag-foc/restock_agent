"""S1-S8 detectors and the fix constructors (FX1-FX3).

Each scanner runs at its own natural grain and emits the common Finding shape; the fix
constructors price a real action so `action_cost` is a rupee figure rather than a percentage of
exposure. See docs/intelligence_layer_design.md 3-4.
"""
